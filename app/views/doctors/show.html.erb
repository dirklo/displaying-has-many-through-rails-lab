<h1><%= @doctor.name %></h1>

<h2>Appointments:</h2>
<ul>
    <% @doctor.appointments.each do |appointment| %>
        <li>
            <%= link_to appointment.patient.name, appointment.patient %> at <%= appointment.appointment_date_and_time %>
        </li>
    <% end %>
</ul>
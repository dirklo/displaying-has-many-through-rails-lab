<%= @patient.name %>

<h2>Appointments</h2>
<ul>
    <%= @patient.appointments.each do |appointment| %>
        <li>
            <%= link_to appointment.doctor.name, appointment.doctor %> at <%= appointment.appointment_date_and_time %>
        </li>
    <% end %>
</ul>